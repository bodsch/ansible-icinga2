---

icinga2_mariadb_packages:
  - mariadb

icinga2_mariadb_build_packages:
  - mariadb-devel
  - gcc
  - gcc-c++
  - autoconf
  - automake
  - python{{ '3' if ansible_python.version.major | int == 3 else '' }}-devel

icinga2_mariadb_python_packages:
  - name: mysqlclient
    # version: 2.0.3

icinga2_os_requirements:
  - iproute

...
